# Dependencies
node_modules/

# Build output (regenerated by `npm run build`; published to npm via prepublishOnly)
dist/

# Test coverage output (regenerated by `npm run coverage`)
coverage/

# npm pack / publish artifacts
*.tgz

# Test coverage output
coverage/

# Large binary media (kept locally; host via GitHub Releases or external CDN if needed)
*.mp4

# Local analytics / scratch
.ghost-panel-analytics.ndjson
.demo-frames/
assets/git images/

# OS / editor
.DS_Store
*.log

# Local-only tooling config
.claude/settings.local.json
